# Slot Machine

## Installation

 ```bash
 npm install
 ```

## Running the App

Start the development server:

```bash
npm run dev
```

The game will be available at `http://localhost:5173`.

## Force win

Press the **`W`** key to toggle "Force Win" mode. The next spin will guarantee a winning line on the middle row.
